# Database (Local Docker)
DATABASE_URL=postgresql://quickdash@localhost:5434/quickdash

# Redis (Local Docker)
REDIS_URL=redis://localhost:6380

# Auth (Better Auth)
BETTER_AUTH_SECRET=dev-secret-change-in-production-minimum-32-characters
BETTER_AUTH_URL=http://localhost:3000

# Google OAuth (get from console.cloud.google.com)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Initial Admin Emails (comma-separated, auto-granted owner on first sign-in)
INITIAL_ADMIN_EMAILS=

# App URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_ADMIN_URL=http://localhost:3001

# Pusher (get from pusher.com)
PUSHER_APP_ID=
PUSHER_KEY=
PUSHER_SECRET=
PUSHER_CLUSTER=
NEXT_PUBLIC_PUSHER_KEY=
NEXT_PUBLIC_PUSHER_CLUSTER=

# Polar (get from polar.sh)
POLAR_ACCESS_TOKEN=
POLAR_WEBHOOK_SECRET=

# Reown (get from dashboard.reown.com)
NEXT_PUBLIC_REOWN_PROJECT_ID=
